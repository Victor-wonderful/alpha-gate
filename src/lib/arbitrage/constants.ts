/** 진입 가능 김프 절대값 슬라이더 기본값 (%). 현재 시장이 역프 ~1.5% 라 3% 권장. */
export const KIMCHI_ENTRY_BAND_DEFAULT_PCT = 3.0;
/** 슬라이더 최솟값. */
export const KIMCHI_BAND_MIN_PCT = 0.5;
/** 슬라이더 최댓값. */
export const KIMCHI_BAND_MAX_PCT = 5.0;

/** 청산 목표 = 진입 김프 + 이 값 (기본 +1.5%p). */
export const KIMCHI_TARGET_OFFSET_PCT = 1.5;
/** 청산 목표 김프 최댓값 (%). */
export const KIMCHI_MAX_TARGET_PCT = 20;
